{
  "name": "copilot-session-handoff-mcp",
  "version": "0.2.0",
  "description": "Standalone MCP server for portable Agency Flow session handoffs.",
  "type": "module",
  "private": true,
  "bin": {
    "copilot-session-handoff-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:plugin": "esbuild src/server.ts --bundle --platform=node --format=esm --target=node20 --outfile=plugin-dist/server.mjs",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && npm run build:plugin && python -m pytest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "fflate": "^0.8.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.1",
    "typescript": "~6.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
